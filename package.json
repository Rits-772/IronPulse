{
  "name": "ironpulse-monorepo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@types/pg": "^8.18.0",
    "dotenv": "^18.0.0",
    "pg": "^8.20.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
